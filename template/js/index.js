$(document).ready(function(){
    Noty.overrideDefaults({
        layout: 'topRight',
        theme: 'nest',
        layout   : 'topRight',
        closeWith: ['click', 'button'],
        timeout: 3000
    });
});